// better approach then creating wrapper components
// global stylesheet approach
import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    bodyText:{
        fontFamily:'open-sans-bold',
        color:'red'
    },
    title:{
        fontFamily:'open-sans',
        fontSize: 18
    }
});